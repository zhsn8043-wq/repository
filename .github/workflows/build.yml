name: Build APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      
      - name: Unzip and explore project structure
        run: |
          unzip -o SudanRescue.zip -d extracted_project
          ls -la extracted_project/
          echo "===== محتويات المجلد بعد فك الضغط ====="
          find extracted_project -type f -name "gradlew" | head -5
      
      - name: Find gradlew and setup project
        run: |
          GRADLEW_PATH=$(find extracted_project -name "gradlew" -type f | head -1)
          if [ -z "$GRADLEW_PATH" ]; then
            echo "❌ لم يتم العثور على ملف gradlew!"
            exit 1
          fi
          echo "✅ تم العثور على gradlew في: $GRADLEW_PATH"
          PROJECT_DIR=$(dirname "$GRADLEW_PATH")
          echo "📂 مجلد المشروع هو: $PROJECT_DIR"
          cp -r "$PROJECT_DIR"/* .
          chmod +x gradlew
      
      - name: Build with Gradle
        run: ./gradlew assembleDebug
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SudanRescue-APK
          path: app/build/outputs/apk/debug/app-debug.apk
